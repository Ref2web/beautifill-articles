{
  "name": "beautifill-articles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "validate:content": "node scripts/validate-content.mjs",
    "export:shopify": "node scripts/export-shopify.mjs",
    "check": "npm run validate:content && npm run export:shopify && npm run build"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.1",
    "astro": "^6.3.8",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.12"
  }
}
